<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="DeprecatedManifestPackage">

    <!-- Permissions for all Android versions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <!-- Camera permission for OCR scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- For Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Internet and network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".PaleoRecipesApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.PaleoRecipes"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Main Compose Activity -->
        <activity
            android:name=".MainActivityCompose"
            android:exported="true"
            android:theme="@style/Theme.PaleoRecipes"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OCR Scan Activity -->
        <activity
            android:name=".OcrScanActivity"
            android:exported="true"
            android:theme="@style/Theme.PaleoRecipes"
            android:parentActivityName=".MainActivityCompose">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivityCompose" />
        </activity>

        <!-- Hilt's work manager configuration -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Production activities only -->
        <activity android:name=".AboutPaleoActivity"
            android:theme="@style/Theme.PaleoRecipes" />
        <activity android:name=".RecipeListActivity"
            android:theme="@style/Theme.PaleoRecipes.ActionBar" />
        <activity android:name=".AddEditRecipeActivity"
            android:theme="@style/Theme.PaleoRecipes" />
        <activity
            android:name=".RecipeDetailActivity"
            android:exported="false"
            android:parentActivityName=".RecipeListActivity"
            android:theme="@style/Theme.PaleoRecipes">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".RecipeListActivity" />
        </activity>

        <activity
            android:name=".FridgeActivity"
            android:exported="false"
            android:parentActivityName=".MainActivityCompose"
            android:theme="@style/Theme.PaleoRecipes">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivityCompose" />
        </activity>

        <activity
            android:name=".ui.search.RecipeSearchActivity"
            android:exported="false"
            android:parentActivityName=".FridgeActivity"
            android:theme="@style/Theme.PaleoRecipes">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".FridgeActivity" />
        </activity>

        <!-- FileProvider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
